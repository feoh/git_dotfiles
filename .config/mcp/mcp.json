{
  "settings": {
    "autoAuth": true
  },
  "mcpServers": {
    "todoist": {
      "command": "npx",
      "args": [
        "-y",
        "@doist/todoist-mcp"
      ],
      "env": {
        "TODOIST_API_KEY": "${TODOIST_API_KEY}"
      },
      "lifecycle": "lazy"
    }
  }
}